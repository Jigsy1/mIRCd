<thisCommand> [-d] <#chan[,#chan,#chan,...]>

This will return a list of users currently on a channel in n!u@h form, along with status they might have.

The -d flag will list all n!u@h hidden because of auditorium mode. (+D)

Example(s):
---------------
/<thisCommand> #<thisNetwork>
/<thisCommand> -d #Conference
